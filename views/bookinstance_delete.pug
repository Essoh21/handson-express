extends layout

block content
  h1 #{title}: #{book_instance.imprint}
  p= book_instance.due_back_formatted 

  
    p Do you really want to delete this bookinstance ?

    form(method='POST' action='')
      div.form-group
        input#instancerid.form-control(type='hidden',name='instanceid', required='true', value=book_instance._id )

      button.btn.btn-primary(type='submit') Delete
